{
  "name": "@ledgerX/types",
  "version": "1.0.0",
  "description": "Shared types for LedgerX",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@repo/typescript-config": "workspace:*",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.1",
    "vitest": "^1.0.0",
    "@vitest/coverage-v8": "^1.0.0"
  }
}